import pinned from './packages.pinned.js';

export { pinned };

const packages = {
	'classnames': true,
	'classnames/dedupe': true,
	'clsx': true,
	'classcat': true,
};

export default packages;

export const append_packages = {};

export const known_issues = {};
